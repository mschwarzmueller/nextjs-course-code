import { useRouter } from "next/router";

const FilteredEvents = () => {
	return (
		<div>
			<h1>Filtered Events</h1>
			<p>This page is where the filtered results of event queries are shown</p>
		</div>
	);
};

export default FilteredEvents;
